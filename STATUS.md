# Project status

Last updated: 2026-09-26 (Asia/Kuala_Lumpur). Record language: English.

## Current checkpoint

- **M1-M6 technical milestones are complete locally. M6 — v0.1 Release Hardening and OSS Readiness — is the latest committed technical milestone, checkpointed on `main` at commit `f966e1b602561044c19eaca6a84cf4baa146b62a` with tree `f4972e15628c75d663b04604d376487b48395897`. Gate 0/1/2 are complete; focused Gate 3 passed with F1/F2 closed, 0 blockers and 0 Should-fix findings; the refreshed candidate passed its own non-live and separately authorized packaged live validation; and exact-path staging verification passed. WinGPUDoctor v0.1 technical development is complete locally. M7 is not required for v0.1. The repository is Public with `main` published, and the Phase E publication gate passed. Phase F security controls are active: Private Vulnerability Reporting is enabled and verified, secret scanning and repository push protection are enabled, and the active `main` ruleset blocks force pushes and deletion. P1.2 is complete: the Phase F documentation reconciliation was committed and pushed as `5354cce40217acb6bd867bc7e408474d37fb9493`, and hosted CI passed 304/304 tests and schema verification on that commit. P1.3, the v0.1.0 release stage, is complete. P1.3A pre-freeze remediation is complete: commit `a9a83c271fa268d836d11f432e1411c403f30b67` passed hosted CI with 305/305 tests and schema verification. The first release source, `bdefbac75dba0e5f65af59c67677e69ad4c7fb27`, was superseded after its package candidate was found to embed the local build path. The P1.3B remediation commit `62e25b7b12d04499f7b39bae417418d33cf297ee` is the final release source **S2** and the `v0.1.0` tag target; it passed hosted CI with 305/305 tests and schema verification. The S2 release candidate `WinGPUDoctor-0.1.0-win-x64.zip` (734729 bytes, SHA-256 `65162553242aea18e5b1cebe963b769de76ebd7f7729d360ae151fa984ce63a8`) passed the final vulnerability audit, the package path guard, non-live validation and separately authorized live validation. WinGPUDoctor v0.1.0 was published on 2026-09-25: the annotated tag `v0.1.0` points exactly to S2, and the public GitHub Release (latest, not a draft or pre-release) has exactly two uploaded assets, that ZIP and its `.sha256`.**
- The production CLI uses the supervisor-backed five-operation path: real operating-system, computer-system, video-controller, conditional signed-driver, and active-topology operations execute in short-lived workers in the established logical order. `WindowsCollector` remains a reference/test aggregate and shares its mapping helpers with the real worker dispatcher.
- Cancellation and output compete for one atomic controller state. Cancellation winning during collection closes result acceptance, prevents later operations, uses bounded worker cleanup and returns `3` before report preparation/preview/export. Output winning permanently closes controlled cancellation before handler removal; an already-dispatched late callback permits ordinary/default termination and cannot create hidden cancellation. Second/reentrant interrupts also permit default termination. No report state or provider timeout is fabricated.
- `CollectionTimingPolicy.CalibratedProduction` is unchanged: overall 60 s, operation 10 s, cleanup 2 s, later-operation reservation 10 s, final bookkeeping 500 ms, connect 2 s and frame 8 s. Inclusive operation, nested terminal-frame wait and cleanup measurements support the choices on one laptop. Connect headroom is engineering judgment without an exactly matched measurement; final bookkeeping is an unmeasured conservative reserve. Fake-clock tests remain the boundary proof.
- Three fresh non-administrator healthy CLI processes on the unchanged internal-display configuration completed with exit `0`, explicit per-run before/after fingerprints and no tracked worker surviving. Schema 0.2.0, privacy projection, exact correlation, report semantics and exit meanings are unchanged. This does not establish cross-machine coverage or same-host admission reuse.
- Package history: the M6 candidates `0328525a466e955686d6680430e3ead6e5a7d3546f7a1d78e53d5e806e8b5295` and `cdefbb10d1f401b3574119255924facece084c16f878081bfcc1183786cdcc8d`, and the first P1.3 candidate `1a319aa63a5f33a8da6d1ce09ed964f556870f147308ab1c34c3ff58614b4a2f` built from `bdefbac…`, remain preserved unchanged under ignored `artifacts/` as historical or pre-fix evidence only. They were not published and must never be published; `cdefbb10…` and `1a319aa6…` are confirmed to embed the local build path. The S2 ZIP `65162553242aea18e5b1cebe963b769de76ebd7f7729d360ae151fa984ce63a8` is the only published package: it and its checksum are the two uploaded v0.1.0 Release assets, and the local copies remain unchanged under ignored `artifacts/`.
- P1.3A pre-freeze remediation (complete; committed as `a9a83c271fa268d836d11f432e1411c403f30b67` on base `5354cce40217acb6bd867bc7e408474d37fb9493` and passed hosted CI): the published synthetic examples had labelled system and driver fields as `wmiVideoController` and listed only video/topology collector runs. The test fixture now uses the production per-field provenance and collector order, a deterministic test compares it with the supervised production path, and the examples were regenerated through the newly tracked `scripts/update-examples.ps1`; they remain wholly synthetic. CLI `--help` and README now state that exit `3` also covers collection stopped with no report (controlled first Ctrl+C or fatal collection failure); exit-code behavior is unchanged. Stale `CONTRIBUTING.md` and `ARCHITECTURE.md` publication/M6 wording was corrected. Production collection, schema 0.2.0, privacy projection and report semantics are unchanged. Owner-accepted non-blocking findings deferred after v0.1.0 are listed in `docs/VALIDATION.md`.
- P1.3B source-path remediation (complete; S2 `62e25b7b12d04499f7b39bae417418d33cf297ee`, parent `bdefbac75dba0e5f65af59c67677e69ad4c7fb27`, exactly 4 files): Release builds set `DeterministicSourcePaths=true`, so first-party debug (CodeView/PDB) paths are rooted at `/_/` instead of the local checkout. The new `scripts/package-path-guard.ps1` makes `scripts/package-v0.1.ps1` fail on the staging folder (before any ZIP is written) or on the final ZIP (before the checksum is written) if a packaged file contains the checkout path, the user-profile path, any `X:\Users\` path, an embedded PDB, or a first-party CodeView path not rooted at `/_/`. Release packaging therefore requires a Git checkout. No product code, dependency, lock file or version changed; `AGENTS.md` notes the mapping.

- **Post-v0.1 GUI scope (M7, Gate 1 accepted; Steps 1-4 checkpointed locally).** On 2026-09-25 the owner accepted Gate 0 for a beginner desktop host and froze ADR 0008 after independent Gate 1 review. The owner separately authorized Step 1. The UI-free `WinGPUDoctor.Host` now owns the scan outcome, per-scan cancellation lifetime and local `CreateNew` export rules; the CLI consumes it while retaining collector injection and its private `M4CollectionProbe`. The test project references Host instead of linking the former CLI `CollectionOutput.cs`; all existing race tests remain and new Host tests cover outcomes, privacy and export rules. An independent read-only review on 2026-09-26 found 0 blockers, 2 should-fix items and 3 nits. As authorized, S1 is fixed (disposing a session during collection now requests controlled cancellation first and releases the controller only after collection ends), N1 is fixed (the public cancellation surface is only `RequestCancellation()`; tests use test-only internal access), and the S2 live cancellation check passed. N2 (exceptions from `PrivacyPolicy.Prepare` propagate from `RunAsync`, unchanged from the CLI; a GUI needs outermost error handling) and N3 (test naming) are recorded without change. On 2026-09-26 the owner authorized, in one batch, a local Step 1 checkpoint commit (`feat(host): extract WinGPUDoctor.Host (M7 Step 1)`) and Steps 2-4 with one local commit per step; push, tag and release are not authorized. The focused independent re-review of the S1 fix recommended by the review follow-up has not been performed. Step 1 is committed locally as `ce4d15bf0ecfee674e661e819b8b96eac6228a5a`. Step 2 adds the WPF host `src/WinGPUDoctor.Desktop` (`wingpudoctor-gui`): Welcome, Scan, summary cards and single-use Cancel over Host, displaying only the report read back from `ReportWriter.Json` (D3), with a WPF-free view-model state machine, resource-based English copy and the built-in Fluent theme (one scoped `WPF0001` suppression). It adds no NuGet package and copies `worker/` like the CLI. Step 2 is committed locally as `f90da1fa4d8eb23389072c97c99a40e9f49d1dcb`. Step 3 adds the resource-based explanation catalog (finding IDs, `WarningCode`, `DataState`, `ReasonCode`, `CollectorStatus`, `DataSource` and a field glossary), findings/report-notes/collection cards, and per-card details with a technical-details toggle, guarded by completeness and reviewed prohibited-claim tests. Step 3 is committed locally as `570a3c421842fcd7f3132b9484a0b609d35f7118`. Step 4 adds the review-and-save panel: the preview is the exact Markdown/JSON writer output of the one retained report, a format change regenerates it, and Save writes exactly that text through the shared Host rules (local fixed drive, `CreateNew`, never overwrite) with the cloud-sync hint; copying from the preview is blocked. Steps 1-4 have not changed the published v0.1.0 release, CLI behavior, schema, privacy projection, rules or worker protocol; the GUI is not packaged. See the dated Step 1, review follow-up and Step 2-4 evidence in `docs/VALIDATION.md`.

## Git state: read before editing

- M1 `7eefff9a995a0d3bfd6ef53e86e7a3b3db4c3bee`; M2/management `d3eaaea347f8c63d67cac493a9640e2baf2c1974`; M3 `16f19ae42f6df6c71f18224cb3e7599fcd05446e`; M4 `57abbec81ac8db83e04a2a588d39fe18c1650070` (parent M3).
- This file records project and milestone state; live Git determines the current branch, HEAD, index, and working tree. M1 tree `fcc13aed94b20fab57acad9749fac1700ba789b3` matches `refs/baselines/milestone-1`; M3 commit tree `f68fbfdbade9c9e7ad63e767423b0efb649b498a`; M4 remains preserved as a historical checkpoint at commit `57abbec81ac8db83e04a2a588d39fe18c1650070` with tree `d9ebe8ae5ee476229bd08b930f4c80d9b0e3f12c`; M5 is checkpointed at commit `43d3bc39a6310a48ca7f819bf307650f3afaa6a1` with tree `e5187c757c29a2f492832dee11730ee9c1b8b404`; and the M6 technical checkpoint is commit `f966e1b602561044c19eaca6a84cf4baa146b62a` with tree `f4972e15628c75d663b04604d376487b48395897`. Later documentation/operating-state commits include `702a1ac43e54bf1a0baf0637ff3ff7d1f9f3d15b` (post-M4 housekeeping) and `dedad9a3940ec3a009433b137d072536ed2f9616` (routing-policy guidance); these do not represent technical milestones.
- The M6 checkpoint was created on `main`, and the immediate post-commit index and working tree were clean. Inspect live Git for current branch, HEAD, index, and working-tree state.
- The M4 checkpoint contains 97 reviewed paths (73 additions and 24 modifications); its commit tree is `d9ebe8ae5ee476229bd08b930f4c80d9b0e3f12c`. M1-M3 history and the M1 baseline ref are unchanged.
- Release source S2 is `62e25b7b12d04499f7b39bae417418d33cf297ee` (`fix: map Release source paths to /_/ and guard packaged paths`, parent `bdefbac75dba0e5f65af59c67677e69ad4c7fb27`), the `v0.1.0` tag target: annotated tag object `0a3da645f624636dce1b5707a79abe3a0fbf987c` points exactly to S2. `bdefbac…` was the superseded first release source. Later documentation-only commits, including the P1.3 evidence record `4d63b2b1dec122dfde897c62c646f755667124b8` and the v0.1.0 publication record, are not part of the release source.
- The private `origin` and first push were created during P1; the first privately pushed `main` commit was `b5a7dc516d532fc3a679b61a4700316c83ffbe72`. Inspect live Git and GitHub for later state. Raw, build, and private validation evidence remains ignored and uncommitted.
- M4 source, projects, tests, scripts and ADR are tracked in the checkpoint. For current staged, unstaged, and untracked files, inspect live Git state. SDK/cache/build output, real reports and local validation evidence remain ignored.

## Verification and limits

M7 Step 4 (2026-09-26): `./scripts/dev.ps1 -Action test` passed outside the sandbox with Release build 0 warnings / 0 errors, **344 passed / 0 failed / 0 skipped** xUnit tests (4 new save tests) and all schema/helper checks. A non-administrator live GUI export through the real Save dialog saved Markdown and JSON files byte-identical to the on-screen preview; the JSON passed the 0.2.0 schema and the existing privacy helper with 0 findings, and a second save to the existing name was refused with the file unchanged. On the final build the repeated-scan/cancel/close smoke passed again and the non-live CLI captures stayed byte-identical. Packaging, missing-runtime, accessibility, system-level no-network/no-other-file and usability checks, and the independent reviews for Steps 2-4, remain outstanding.

M7 Step 3 (2026-09-26): `./scripts/dev.ps1 -Action test` passed outside the sandbox with Release build 0 warnings / 0 errors, **340 passed / 0 failed / 0 skipped** xUnit tests (8 new catalog tests: enum, Core finding-ID and connector-token completeness, XAML key coverage, prohibited-claim review and detail content) and all schema/helper checks. A live UI Automation pass on this laptop expanded all detail sections for a local visual check. The copy review so far is the implementer's pattern list, not the independent Step 6 review.

M7 Step 2 (2026-09-26): `./scripts/dev.ps1 -Action test` passed outside the sandbox with Release build 0 warnings / 0 errors, **332 passed / 0 failed / 0 skipped** xUnit tests (15 new Desktop tests, including the D3 JSON round-trip over 7 fixtures and the scan/cancel/restart/close state machine) and all schema/helper checks. A non-administrator live GUI smoke on this laptop completed two scans in one process (first live same-host admission reuse), one Cancel ending in "Scan stopped", and one close during a scan that exited `0`, with no surviving worker. Accessibility, high-contrast, scaling, packaging and usability checks have not been run.

M7 Step 1 (2026-09-25): `./scripts/dev.ps1 -Action test` passed outside the sandbox: Release build 0 warnings / 0 errors, **313 passed / 0 failed / 0 skipped** xUnit tests, schema checks PASS, and the existing helper/admission/deployment/fingerprint/process checks passed. Before/after Release CLI captures were byte-identical for `--help`, live default preview, noninteractive export decline and interactive `NO` decline, including stdout, stderr and exit codes (`0`, `0`, `4`, `4`); declined destinations were not created. This is one laptop/current configuration only, not a repeated-scan GUI or live cancellation validation. No GUI or new NuGet package was added; nothing was staged, committed or pushed.

M7 Step 1 review follow-up (2026-09-26, S1/N1 fixes and the authorized S2 live check): `./scripts/dev.ps1 -Action test` passed outside the sandbox with Release build 0 warnings / 0 errors, **317 passed / 0 failed / 0 skipped** xUnit tests (4 new Host lifecycle/API tests), schema checks PASS and all helper checks passed; a temporary pre-fix `Dispose()` mutation made the two disposal regression tests fail. Non-live Release CLI captures (`--help` and four argument errors) were byte-identical before and after, including exit codes. The owner-authorized `./scripts/test-m4-cancel.ps1` passed one non-administrator live run: exit `3` 33.3 ms after a real `CTRL_C_EVENT` during validated `wmi.displayDrivers` attempt 1, one cancellation observed, no later worker launch, all four workers cleaned up with no survivor, no report or preview, and the notice bytes unchanged from earlier live runs. Disposal during collection is deterministic-only; the S1 fix has not yet been independently re-reviewed.

v0.1.0 publication (2026-09-25, one owner-run release batch with GitHub CLI 2.101.0 from a clean `main` at the P1.3 evidence commit `4d63b2b1dec122dfde897c62c646f755667124b8`, whose hosted CI run #9 passed 305/305 tests and schema verification with no uploaded artifact; details in [docs/VALIDATION.md](docs/VALIDATION.md)):

- **Tag.** Annotated tag `v0.1.0` (tag object `0a3da645f624636dce1b5707a79abe3a0fbf987c`) points exactly to S2 `62e25b7b12d04499f7b39bae417418d33cf297ee`. It was verified before pushing, only the tag was pushed, and it is the repository's only tag.
- **Release.** "WinGPUDoctor v0.1.0" was created from the existing tag as a draft, its uploaded assets were downloaded back and verified, and it was then published at 2026-09-25 11:48:14 UTC. It is public, marked latest, not a draft or pre-release, and the repository's only Release.
- **Assets.** Exactly two uploaded assets: `WinGPUDoctor-0.1.0-win-x64.zip` (734729 bytes, SHA-256 `65162553242aea18e5b1cebe963b769de76ebd7f7729d360ae151fa984ce63a8`) and `WinGPUDoctor-0.1.0-win-x64.zip.sha256`. Public copies were downloaded without authentication and re-hashed on the owner's machine and, independently, in a separate review; all matched, as did GitHub's reported asset digests. The "Source code" zip and tar.gz are archives GitHub generates automatically from the tag, not uploaded assets.
- **Unchanged.** `main` stayed on the post-validation docs commit `4d63b2b…` throughout publication with a clean tree, the tag stayed on S2, and the local ZIP and checksum were unchanged. The pre-fix `1a319aa6…` and historical M6 ZIPs were not published.

P1.3 final release evidence (2026-09-25, owner-run on the same Windows 11 x64 laptop, build 26200, PowerShell 7.6.6, checkout at S2 with a clean tree before and after each batch; details in [docs/VALIDATION.md](docs/VALIDATION.md)):

- **S2 hosted CI.** Run #8 on the exact SHA: Release build 0 warnings / 0 errors, **305 passed / 0 failed / 0 skipped** as a standard user, schema checks PASS, 0 uploaded artifacts.
- **Final audit.** 7 projects, **0 vulnerable direct or transitive packages**, 0 problems, all 7 lock files unchanged.
- **Release candidate.** Built after explicit-path deletion of the 7 projects' `bin`/`obj` with 0 warnings / 0 errors: `WinGPUDoctor-0.1.0-win-x64.zip`, 734729 bytes, SHA-256 `65162553242aea18e5b1cebe963b769de76ebd7f7729d360ae151fa984ce63a8`, checksum file verified, 25 files, 9 Worker files, single package root, packaged documents byte-identical to S2.
- **Path guard.** 0 findings on the staging folder and 0 on the final ZIP; all 9 first-party CodeView paths are rooted at `/_/`, with no local checkout or user-profile path.
- **Comparison with the pre-fix `1a319aa6…` package.** Exactly the 9 first-party DLLs changed. Under the corrected Gate 6b rule, their differences are limited to debug data, MVID/timestamp/layout offsets and the Supervisor's regenerated Worker manifest (Worker length, SHA-256 and MVID pins), and every regenerated pin matches the final Worker files.
- **Step 4a** (non-live, fresh extraction) PASS.
- **Batch 4 live** (non-administrator, Medium integrity) PASS: `verify-cli.ps1` 7/7, direct preview and explicit export with schema, privacy and report-content checks PASS, all 5 collectors succeeded, and one controlled first Ctrl+C exited `3` with every tracked Worker cleaned up and 0 survivors.
- **Final state.** ZIP, checksum and preserved evidence unchanged; S2 unchanged; tracked tree clean.

The v0.1.0 binaries are unsigned (Authenticode `NotSigned`); the published Release notes disclose this and give the SHA-256 verification steps. The live report is local-only ignored evidence and is not committed or published.

P1.3A evidence (2026-09-25, owner-run in the local Windows 11 x64 checkout on base `5354cce40217acb6bd867bc7e408474d37fb9493`, PowerShell 7.6.6; details in [docs/VALIDATION.md](docs/VALIDATION.md)): Release build passed; `scripts/update-examples.ps1` regenerated the examples with schema checks passing and SHA-256 values matching the independently predicted output; `./scripts/dev.ps1 -Action test` passed **305 passed / 0 failed / 0 skipped** xUnit tests, synthetic schema checks, 49 M3 helper checks, 8 M4 integrated schema checks, 1 admission probe, 10 deployment checks, 8 execution-fingerprint checks and 6 process-evidence checks; the Release CLI `--help` showed the clarified exit-code wording with exit `0`; `git diff --check` exited `0`. No live collection, packaged or physical-hardware check was run. The pushed remediation commit `a9a83c271fa268d836d11f432e1411c403f30b67` then passed hosted GitHub Actions `deterministic-tests` run #6 as a standard user: **305 passed / 0 failed / 0 skipped** and schema checks PASS, with no uploaded artifact; the only annotation was the known non-failing Node.js 20 deprecation notice.

Fresh M6 evidence is in [docs/VALIDATION.md](docs/VALIDATION.md). Gate 2 passed locked Release builds in this checkout and a fresh source copy with 0 warnings/0 errors; **304 passed / 0 failed / 0 skipped** xUnit tests and all schema/helper checks in this checkout; separate advisory audit; package ZIP/checksum and all 25 entries; and non-live extracted-package checks. On 2026-09-24, the original ZIP (`0328525a466e955686d6680430e3ead6e5a7d3546f7a1d78e53d5e806e8b5295`) passed its separately authorized non-administrator live session; two restricted-process attempts first failed without a separately proven internal cause. After Gate 3 F1/F2 correction, the refreshed ZIP (`cdefbb10d1f401b3574119255924facece084c16f878081bfcc1183786cdcc8d`) passed a Release build, non-live checks and its **own** non-administrator packaged live CLI verification, explicit JSON export, schema/privacy/evidence review and one first-Ctrl+C cancellation with no report or surviving tracked Worker. Refreshed physical coverage is one laptop/current session and configuration only. Tool version is 0.1.0; report schema remains 0.2.0. Private Vulnerability Reporting is now enabled and verified; the existing local ZIP remains historical evidence, not a final release asset.

Correction-pass automated evidence is recorded in [docs/VALIDATION.md](docs/VALIDATION.md): locked restore; Release build with 0 warnings/0 errors; xUnit: **304 passed / 0 failed / 0 skipped**; synthetic schema positive/negative checks; 8 integrated schema fixtures; 49 M3 helpers; 1 M4 admission probe; 10 deployment checks; 8 execution-fingerprint checks; 6 M4 process-evidence checks; and git diff --check passed. The focused M5 re-review passed **34 tests** and independently closed F1. The build, test, schema and helper evidence above was inspected, not rerun, during the documentation-only reconciliation. A fresh git diff --check passed after that update. The separate Gate 4 live check passed on this laptop with exit 0, five successful collection operations, one active path and exact source/target associations; its narrow scope and untested cases are recorded in docs/VALIDATION.md. At that M5 validation-record snapshot, M5 was the latest committed technical milestone; physical coverage remains limited to this laptop.

Historical M4 correction evidence includes **270/270** tests, 7 CLI checks, 3/3 healthy collections, 3/3 corrected live Ctrl+C runs and a non-signalled control. Those are not refreshed M5 measurements. The M4 checkpoint-readiness review passed; its private evidence remains ignored.

- Cancellation: six new CLI-boundary race cases use gates, including a captured callback before/after commitment, in-flight token signaling, reentrancy and output suppression. Existing late-result, no-later-operation and cleanup tests remain. Live readiness means the parent validated Ready, Start and attempt 1 for `wmi.displayDrivers`; PID plus birth identities are retained and checked independently after CLI exit. The three signal-call-to-observed-exit durations were 79.8221, 39.0612 and 45.7510 ms, with exit `3`, no preview/export and no later worker launch in the trace. The second forced interrupt remains deterministic-only.
- Timing: the historical 4.299 s inclusive operation contains the 4.002 s terminal-frame wait and cleanup; these are not additive. The 0.111 s first Ready receive is not the connect stage. Cleanup 28.02 ms supports a roughly 71-fold allowance, not a bookkeeping claim. Whole-CLI fresh durations 6615, 6418 and 6475 ms include startup/output and do not measure operation headroom.
- Format-2 fingerprints matched before/after each of the three current runs and against that batch's explicit baseline. No claim equates the corrected build with an older build. The metadata-only fingerprint worker performs no hardware collection; the separate opt-in lifecycle probe records process metadata only into ignored evidence. Same-host admission reuse/poison claims come from deterministic tests, not separate CLI processes.
- The historical M5 deterministic restore skipped vulnerability auditing. M6 Gate 2 ran the separate `dev.ps1 -Action audit` successfully with advisory access. The first hosted `deterministic-tests` run on `b5a7dc5` reported 294 passed / 10 failed before schema verification; the CI compatibility correction and local revalidation are recorded in `docs/VALIDATION.md`. The corrected hosted run on `7ae583a9bd34d59b473ea62303b2267542556a9d` passed 304/304 tests and schema verification, with no artifact upload.
- GitHub Dependency Graph and Dependabot alerts are enabled on the Public repository. GitHub currently lists 6 direct dependencies and 0 open alerts, but does not represent the 7 tracked NuGet lock files' transitive set completely. A local .NET 10.0.401 direct-and-transitive audit of the existing locked dependency state reported no known vulnerable packages. P1.3 reran that audit on S2 as the v0.1.0 release gate: 7 projects, 0 vulnerable direct or transitive packages, 0 problems. Dependency-submission automation and Dependabot version-update automation are deferred maintenance, not publication blockers. See the dated P1.2 evidence in `docs/VALIDATION.md`.
- Live validation exercised only this laptop with a single active internal display path at 2560 × 1600. External displays, clone/extended/hot-plug, AMD, ARM64, RDP, virtual/eGPU and other machines remain unverified; timing and cancellation results do not extend to them.
- `host-cancelled` stays an internal supervisor code. Schema remains 0.2.0, no new exit code was added, and controlled cancellation deliberately produces no report.
- The managed pipe wrapper retains operation/cancellation/handle ownership, bounds completion observation and quarantines unresolved sessions until host exit. Exactly two handles are inherited: the pipe client for stdin/stdout and a write-only NUL for stderr. A current-user DACL is not authentication against hostile same-user processes.

## Next action

M1-M6 technical development and the local v0.1 checkpoint are complete; M7 is not required for v0.1. P1.2 is complete: the Phase F documentation reconciliation was committed and pushed as `5354cce40217acb6bd867bc7e408474d37fb9493`, with hosted CI passing 304/304 and schema verification; at the start of P1.3A, `main` and `origin/main` were both at that commit. P1.3A pre-freeze remediation is complete: `a9a83c271fa268d836d11f432e1411c403f30b67` passed local Windows validation and hosted CI with 305/305 tests and schema verification. P1.3 is complete. Release validation passed for S2 `62e25b7b12d04499f7b39bae417418d33cf297ee` and its ZIP (SHA-256 `65162553242aea18e5b1cebe963b769de76ebd7f7729d360ae151fa984ce63a8`), and v0.1.0 was published on 2026-09-25: the annotated `v0.1.0` tag points exactly to S2, and the public, latest GitHub Release has exactly two uploaded assets, that ZIP and its `.sha256`. The P1.3 evidence record `4d63b2b1dec122dfde897c62c646f755667124b8` and the v0.1.0 publication record are docs-only commits after S2 and are not part of the release source. No tag, Release or asset step remains pending; any separate announcement needs its own authorization. The pre-fix and historical ZIPs were not published and must never be, and the v0.1.0 binaries are unsigned. Code signing, any new feature or broader hardware support is future post-v0.1 work and must be separately scoped rather than added to M6. Reconstruct current branch, HEAD, index and worktree from live Git before further work.

M7 next action: the authorized batch (Steps 1-4) is complete and checkpointed locally; nothing is pushed. Before Step 5, obtain independent reviews per `AGENTS.md`: a focused re-review of the Step 1 S1 disposal/cancellation fix and N1 API change, and a review of the Steps 2-4 GUI lifecycle (scan/cancel/close state machine), privacy/export boundary (D3 round-trip, preview-equals-save, Host export use) and UI copy. Steps 5-6 (packaging with allowlist/path-guard updates, then live, accessibility and usability validation and the copy review) need separate owner authorization. The GUI's outermost `RunAsync` error handling (review N2) is implemented in `MainViewModel`. The Step 1 implementation review on 2026-09-26 found 0 blockers, 2 should-fix items and 3 nits; the earlier Gate 1 architecture review passed with 0 blockers, 5 should-fix items and 1 nit, addressed before ADR 0008 was frozen.

## Resume

Read AGENTS.md -> this file -> live Git status/diff/history -> relevant code/tests/ADRs. Live Git determines current branch, HEAD, index and working tree; dated validation and review records describe their own snapshots. M6 is the latest technical checkpoint, anchored by commit `f966e1b602561044c19eaca6a84cf4baa146b62a` and tree `f4972e15628c75d663b04604d376487b48395897`. M1-M6 and local v0.1 technical development are complete; M7 is not required for v0.1. The repository is Public and Private Vulnerability Reporting is enabled and verified. P1.2, P1.3A, P1.3B, P1.3 release validation and v0.1.0 publication are complete. Release source S2 is `62e25b7b12d04499f7b39bae417418d33cf297ee`, and the annotated `v0.1.0` tag points exactly to it. The public v0.1.0 GitHub Release has exactly two uploaded assets: the validated ZIP (SHA-256 `65162553242aea18e5b1cebe963b769de76ebd7f7729d360ae151fa984ce63a8`) and its `.sha256`.
