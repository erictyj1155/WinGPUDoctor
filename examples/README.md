# Synthetic examples

`report.example.json` and `report.example.md` contain deliberately invented hardware names, IDs, versions, and manufacturer/model values. They are generated from the same synthetic snapshot through the actual privacy boundary and exporters. They are format examples, not measurements from a real machine.

Real smoke-check reports are kept only in ignored `artifacts/`. Never replace these examples with a machine dump.
